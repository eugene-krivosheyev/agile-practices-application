<!--
Stub logback config to make Maven build less verbose.
Make run configuration in IDEA:
```
mvn clean verify -Dlogback.configurationFile=logback.xml
```
Spring will use logback-spring.xml anyway.
-->
<configuration/>
